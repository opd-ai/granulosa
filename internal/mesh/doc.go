// Package mesh manages the set of active WireGuard peers, tracks their
// transport.NetworkAddress endpoints, and keeps the WireGuard device
// configuration in sync with DHT-resolved peer information.
package mesh
